# Parallel-System-Final-Project
This project focuses on accelerating the Tensor-Times-Matrix (TTM) operation, a core component of tensor algebra, using CUDA. It compares GPU-based performance against CPU implementations (OpenMP, MPI), achieving significant speedups for large-scale, high-dimensional tensor data.
